import {describe,it} from "vitest";
import fun from '../../api/related'

describe('fdssf', () => {
    it('should ', async() => {
        //@ts-ignore
        const results = await fun({query:{term:'css'} }, {json:console.log})


    });
});