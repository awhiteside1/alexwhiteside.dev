---
import type { CollectionEntry } from 'astro:content'
import BentoGridItem from '@/ui/astro/components/bento/BentoGridItem.astro'
import { optimizeImage } from '@ui/utils/optimizeImage'
import StandardCard from './cards/StandardCard.astro'
type Props = {
    books?: CollectionEntry<'awesome'>[]
}

const { books = [] } = Astro.props
---

{books.map((entry) => <StandardCard entry={entry} />)}
